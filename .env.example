# Environment variable names go here as the project gains them, one per line,
# with placeholder values only. Real values live in .env, which is never
# committed, and in the deploy provider's interface. See docs/security-and-data.md.
# EXAMPLE_API_KEY=your-key-here
