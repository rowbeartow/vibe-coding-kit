Read `docs/agent-rules.md` first. It is the canonical behavior spec for this repository: change protocol, stop conditions, two-failure rule, session protocol.

Project specifics (stack, commands, constraints, docs index) live in `CLAUDE.md`. Despite the filename, those sections apply to every agent.
